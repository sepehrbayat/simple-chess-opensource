# راهنمای سریع - Chess Project

## ✅ وضعیت نصب

Python و تمام وابستگی‌ها با موفقیت نصب شده‌اند!

- **Python:** 3.12.10
- **Pygame:** 2.6.1
- **PyInstaller:** 6.17.0
- **محیط مجازی:** فعال در پوشه `venv`

---

## 🚀 اجرای بازی

### روش 1: استفاده از اسکریپت (ساده‌ترین روش)

**PowerShell:**
```powershell
.\run.ps1
```

**Command Prompt:**
```cmd
run.bat
```

### روش 2: اجرای دستی

**PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
python main.py
```

**Command Prompt:**
```cmd
venv\Scripts\activate.bat
python main.py
```

---

## 📝 نکات مهم

1. **همیشه محیط مجازی را فعال کنید** قبل از اجرای بازی
2. **غیرفعال‌سازی محیط مجازی:** تایپ کنید `deactivate`
3. **اگر Python در PATH نیست:** PowerShell را بسته و دوباره باز کنید

---

## 🔧 در صورت بروز مشکل

اگر مشکلی پیش آمد:

1. مطمئن شوید Python در PATH است:
   ```powershell
   python --version
   ```

2. محیط مجازی را دوباره ایجاد کنید:
   ```powershell
   .\setup.ps1
   ```

3. وابستگی‌ها را دوباره نصب کنید:
   ```powershell
   .\venv\Scripts\Activate.ps1
   pip install -r requirements.txt
   ```

---

## 📦 ساخت فایل اجرایی (اختیاری)

برای ساخت یک فایل `.exe` قابل اجرا:

```powershell
.\venv\Scripts\Activate.ps1
pyinstaller --onefile --windowed --name ChessMVP main.py
```

فایل اجرایی در پوشه `dist` ایجاد می‌شود.

---

**لذت ببرید! 🎮**
